using UnityEngine;
using System.Collections;

public class OtherUserAudioPlay : MonoBehaviour {
	/*
	AudioSource audioSource;

	void Awake () {
		// This code is activated to other users...
		if (GetComponent<PhotonView> ().isMine)
			this.enabled = false;
		else
			audioSource = GetComponent<AudioSource> ();
		// Change ragdoll.
	}
	*/
}
